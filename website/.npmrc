# Do not allow npm audit fix to change version ranges automatically.
# The serialize-javascript vulns are build-tool only (webpack internals)
# and do not affect the deployed static site. Wait for Docusaurus upstream.
audit=false
fund=false
